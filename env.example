# Rustodo Environment Configuration
# 
# Instructions:
# 1. Copy this file: cp env.example .env
# 2. Edit the .env file with your preferred values
# 3. Run: docker compose up -d
#
# Note: The .env file is git-ignored and will not be committed to version control

# ============================================================
# MySQL Database Configuration
# ============================================================
# Change these to secure values for production use
# These credentials will be used to create the MySQL database
MYSQL_ROOT_PASSWORD=rustodo_root_password
MYSQL_USER=rustodo_user
MYSQL_PASSWORD=rustodo_password

# ============================================================
# Port Configuration
# ============================================================
# Change these if the default ports are already in use on your system
# Example: If you have another app using port 3000, change BACKEND_PORT to 3001

# Backend API port (exposed to host)
BACKEND_PORT=3000

# Frontend web app port (exposed to host)
FRONTEND_PORT=8080

# MySQL database port (exposed to host)
MYSQL_PORT=3306

# ============================================================
# Backend Configuration
# ============================================================
# Logging level: trace, debug, info, warn, error
RUST_LOG=info

# ============================================================
# Frontend API URL
# ============================================================
# This tells the frontend where to find the backend API
# ⚠️ IMPORTANT: If you change BACKEND_PORT above, update this to match
# Example: If BACKEND_PORT=3001, set this to http://localhost:3001
VITE_API_BASE_URL=http://localhost:3000

